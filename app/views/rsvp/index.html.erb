<h1>RSVP</h1>

Please RSVP by XX.
<p>
<% if flash[:notice] %>
<%= flash[:notice] %>
<% end %>

<% if @rsvp.errors.any? %>
  You couldn't complete your RSVP because <%= pluralize(@rsvp.errors.count, "error")%> occurred.
  <ul>
    <% @rsvp.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<p>
<%= form_for @rsvp, :url => {action: "create"} do |f| %>
<div class="form-group">
  <%= f.label :first_name, 'First Name:' %>
  <%= f.text_field :first_name %>
</div>

<div class="form-group">
  <%= f.label :last_name, 'Last Name:' %>
  <%= f.text_field :last_name %>
</div>

<div class="form-group">
  <%= f.label :email, 'Email:' %>
  <%= f.text_field :email %>
</div>

<div class="form-group">
  <%= f.label :rsvp, 'RSVP:' %>
  <%= f.label :rsvp_yes, 'Able to attend'%>
  <%= f.radio_button :rsvp, 'yes' %>
  <%= f.label :rsvp_no, 'Unable to attend'%>
  <%= f.radio_button :rsvp, 'no' %>
</div>

<div class="form-group">
  <%= f.label :dietary_requirements, 'Dietary Requirements:' %>
  <%= f.text_field :dietary_requirements %>
</div>

<div class="form-group">
  <%= f.label :anything_else, 'Anything else?' %>
  <%= f.text_field :anything_else %>
</div>

<br>
<div class="form-group">
  <%= f.submit "Submit" %>
</div>
<% end %>
